<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page"
    android:fillViewport="true"
    tools:context=".ui.plan.PlanGeneratorFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:paddingBottom="80dp">

        <!-- 城市选择器 -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/til_city"
            style="@style/Widget.Material3.TextInputLayout.FilledBox.ExposedDropdownMenu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="选择城市"
            android:layout_marginBottom="16dp">

            <com.google.android.material.textfield.MaterialAutoCompleteTextView
                android:id="@+id/actv_city"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="none"
                android:textSize="15sp" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- 顶部标语 -->
        <TextView
            android:id="@+id/tv_slogan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="周末不知道去哪？"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            android:layout_marginTop="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="让不期周末帮你安排！"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/nav_bg"
            android:layout_marginBottom="24dp" />

        <!-- 一键生成按钮 - 蓝色渐变风格 -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/btn_generate_plan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/primary_color"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp"
            android:foreground="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:layout_marginBottom="24dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:padding="28dp">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_launcher_foreground"
                    android:contentDescription="生成图标" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="开启不期周末"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:layout_marginTop="12dp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="基于你的偏好，智能生成专属行程"
                    android:textSize="13sp"
                    android:textColor="#CCFFFFFF"
                    android:layout_marginTop="6dp" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- 加载指示器 -->
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:indeterminateTint="@color/primary_color"
            android:layout_marginBottom="16dp" />

        <!-- 错误提示 -->
        <TextView
            android:id="@+id/tv_error"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/error_color"
            android:textSize="14sp"
            android:gravity="center"
            android:padding="8dp"
            android:visibility="gone"
            android:background="@drawable/bg_error_rounded" />

        <!-- 生成的方案展示区（时间轴卡片流） -->
        <TextView
            android:id="@+id/tv_plan_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="今日行程安排"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/nav_bg"
            android:visibility="gone"
            android:layout_marginBottom="12dp"
            android:layout_marginTop="8dp" />

        <LinearLayout
            android:id="@+id/layout_plan_items"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone" />

        <!-- 方案操作按钮组 -->
        <LinearLayout
            android:id="@+id/layout_actions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:visibility="gone"
            android:layout_marginTop="20dp">

            <!-- 换个感觉 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_regenerate"
                style="@style/Widget.Material3.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:text="换个感觉"
                android:textSize="14sp"
                android:textColor="@color/primary_color"
                app:strokeColor="@color/primary_color"
                android:layout_marginEnd="8dp" />

            <!-- 保存方案 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_save"
                style="@style/Widget.Material3.Button"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:text="保存方案"
                android:textSize="14sp"
                android:layout_marginStart="8dp"
                app:backgroundTint="@color/primary_color" />

        </LinearLayout>

        <!-- 发布为盲盒按钮 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_publish_blindbox"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:text="发布为盲盒，找伙伴一起玩！"
            android:textSize="14sp"
            android:visibility="gone"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="72dp"
            app:strokeColor="@color/accent_color"
            android:textColor="@color/accent_color" />

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
